import { storeFactor } from '../test/testUtils';
import { guessWord } from './actions'; // guessWord or guessedWord?

describe ('guessWord action dispatcher', () => {
  describe('no guessed words', ()=> {
    test('updates state correctly for unsuccessful guessedLetterSet', () => {

    });
  });

  describe('no guessed words', ()=> {
    test('updates state correctly for successful guessedLetterSet', () => {

    });
  });

  describe('some guessed words', ()=> {
    test('', () => {

    });
  });

  describe('some guessed words', ()=> {
    test('', () => {

    });
  });

});
